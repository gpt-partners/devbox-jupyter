version: "3"
services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8888:8888
      - 8866:8866
    volumes:
      - ./data:/root/app
      - ./jupyter-config:/root/.jupyter
